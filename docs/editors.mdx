---
title: Editors
description: Optional TypeScript diagnostics and syntax highlighting
---

UseClassy works in **any editor** once the Vite plugin is configured. Editor extras are optional — they improve what you see while typing. Dev and build do not require them.

After `npm i -D vite-plugin-useclassy`, assets ship inside the package:

| Asset | Path under `node_modules/vite-plugin-useclassy/` |
| --- | --- |
| VS Code extension | `vscode-useclassy/` |
| Neovim runtime | `editor/neovim/` |
| Zed settings template | `editor/zed/settings.example.json` |

## TypeScript diagnostics (React)

Add the language plugin (`init` does this for React):

```json
{
  "compilerOptions": {
    "plugins": [{ "name": "vite-plugin-useclassy/typescript-plugin" }]
  }
}
```

If your app uses Vite’s split config (`tsconfig.json` with `"files": []` referencing `tsconfig.app.json`), make the root config extend the app config instead — otherwise tsserver stops at the empty file list and never loads the plugin:

```json
{
  "extends": "./tsconfig.app.json",
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

Keep `build` on `tsc -p tsconfig.node.json && vite build` so CLI `tsc` does not parse modifier attrs without the plugin.

In VS Code / Cursor, use the workspace TypeScript version (`js/ts.tsdk.path`: `node_modules/typescript/lib`, `js/ts.tsserver.useSyntaxServer`: `never`, enable “Use Workspace Version”). Oxlint and ESLint still parse source text directly, so keep smoke-demo `App.tsx` files ignored unless you add a separate ESLint preprocessor.

## Syntax highlighting

The TypeScript plugin only affects diagnostics. For exotic names like `className:@md`, `className:group-hover/item`, and bracket variants:

### VS Code / Cursor / VSCodium / Windsurf

Not on the Marketplace yet. Sideload from `node_modules`:

```bash
# After: npm i -D vite-plugin-useclassy
code --install-extension ./node_modules/vite-plugin-useclassy/vscode-useclassy
# same path for: cursor, codium, code-insiders, windsurf
```

Reload the window after installing.

### Neovim

Add the package’s Neovim folder to `'runtimepath'`:

```lua
vim.opt.rtp:append(
  vim.fn.getcwd() .. '/node_modules/vite-plugin-useclassy/editor/neovim'
)
```

Pair with the tsconfig plugin and `ts_ls` `pluginPaths: { './node_modules' }`.

### Zed

Copy `node_modules/vite-plugin-useclassy/editor/zed/settings.example.json` to `.zed/settings.json` for vtsls + the tsconfig language plugin. Syntax highlighting for exotic modifiers is not available in Zed yet.

### JetBrains

No plugin yet — rely on Vite and ignore JSX attribute warnings on exotic modifiers.

Full detail lives in the repo under [`editor/`](https://github.com/jrmybtlr/useclassy/tree/main/editor).
